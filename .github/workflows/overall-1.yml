name: Overall 1
on: workflow_dispatch
jobs:
  first:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::error::error output"
          echo "::warning::warning output"
          echo "::notice::notice output"
